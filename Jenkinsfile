pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                    echo 'inside git hub'
                  }
       }
             stage('Build') {
             steps {
                    echo 'inside git again'
                  }
              }
    }
}
